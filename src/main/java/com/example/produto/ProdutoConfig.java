package com.example.produto;

import java.util.List;

import org.springframework.boot.CommandLineRunner;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

//Classe para montar bancos de dados dummy para testes
@Configuration
public class ProdutoConfig {

	/*
	 * @Bean CommandLineRunner commandLineRunner(ProdutoRepository repository) {
	 * return args -> { Produto panela = new Produto("Panela", "Nao gruda", 100, 2);
	 * Produto torradeira = new Produto("Torradeira", "Esquenta bem", 250, 10);
	 * 
	 * repository.saveAll(List.of(panela, torradeira)); }; }
	 */
}
